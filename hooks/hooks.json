{
  "description": "Lean Agile Team hooks for task validation and agent lifecycle",
  "hooks": {
    "TaskCompleted": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before marking this task as completed, verify: (1) All acceptance criteria from the task description are met — if claude-progress.json exists, cross-reference its acceptance criteria. (2) No partial implementations or TODO placeholders remain. (3) If code was changed, it compiles/runs without errors. (4) Grade the output on concrete, measurable criteria rather than subjective impressions. If any criteria are not met, reject the completion and explain what remains."
          }
        ]
      }
    ],
    "TeammateIdle": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const os=require('os'),fs=require('fs'),p=require('path');fs.appendFileSync(p.join(os.tmpdir(),'lean-agile-team.log'),'['+new Date().toTimeString().slice(0,8)+'] Teammate idle: '+process.env.CLAUDE_TEAMMATE_NAME+'\\n');\""
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "shipper",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "The shipper agent has finished. Validate git state: (1) No uncommitted changes (git status is clean). (2) No failed pushes or unresolved merge conflicts. (3) Current branch is in a consistent state. Report any issues found."
          }
        ]
      }
    ]
  }
}
